# 0002 — Legacy retry shim

Status: accepted
Concerns-paths: harness_labs/graphrun/legacy_retry_shim.py
Date: 2026-01-10
Owners: Harness Labs maintainers
Run: not applicable

## Context

A fixture decision for the improvement-program tests
(`tests/test_improvement_program.py`). `0003-supersedes-legacy-retry-shim.md`
lists this decision in its `Supersedes` field while this record itself is
still `Status: accepted` — the deliberate, unresolved contradiction that
`decision_registry.DecisionRegistry.active_decisions_for_paths` reports as
an `Inconsistency` rather than resolving silently.

## Decision

Fixture only; not a real repository decision.

## Validation and reversal

Fixture only; not a real repository decision.
